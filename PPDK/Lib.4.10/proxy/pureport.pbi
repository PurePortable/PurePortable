
;DeclareProxyDll(pureport)

ProcedureDLL PurePortable(id.l,*param1,*param2,reserved) : ProcedureReturn 0 : EndProcedure ; для того, чтобы была хоть одна экспортируемая функция (для setdll.exe)

; IDE Options = PureBasic 6.04 LTS (Windows - x86)
; CursorPosition = 1
; Folding = -
; EnableThread
; DisableDebugger
; EnableExeConstant